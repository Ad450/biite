import 'package:flutter/material.dart';

class TotalRequestExpanded extends StatelessWidget {
  const TotalRequestExpanded({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
