import 'package:flutter/material.dart';

class ProjectPropositions extends StatelessWidget {
  const ProjectPropositions({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
