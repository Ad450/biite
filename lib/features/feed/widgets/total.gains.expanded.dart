import 'package:flutter/material.dart';

class TotalGainsExpanded extends StatelessWidget {
  const TotalGainsExpanded({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
