import 'package:flutter/material.dart';

class TotalProjectsExpanded extends StatelessWidget {
  const TotalProjectsExpanded({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
